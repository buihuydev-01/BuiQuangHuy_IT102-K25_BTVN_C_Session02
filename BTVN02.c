#include <stdio.h>

int main(void) {
    /* Khai báo và khởi tạo hai biến số nguyên */
    int a = 5;
    int b = 7;

    /* Tính tổng và lưu vào biến khác */
    int sum = a + b;

    /* In kết quả ra màn hình */
    printf("a = %d\nb = %d\nTong = %d\n", a, b, sum);

    return 0;
}